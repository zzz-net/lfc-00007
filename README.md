# 值班交接白板

一个本地运行的运维班组值班交接白板系统，支持事项管理、交接班确认、历史记录查询等功能。

## 功能特性

- 📋 **看板式管理**：五列看板（新建/处理中/待接班确认/已接收/已关闭）直观展示事项状态
- ⚙️ **灵活配置**：支持班次、角色、用户、交接模板和检查项的自定义配置
- 🔄 **事项流转**：完整的状态流转流程，每次操作都有历史记录
- 🛡️ **权限控制**：基于角色的权限管理，不同角色有不同操作权限
- 📝 **版本控制**：乐观锁机制，两次基于同一版本的编辑会提示冲突
- ✅ **必填校验**：缺少必填检查项时不能进行接班确认
- 📤 **交接单导出**：支持导出文本格式的交接单
- 📜 **历史记录**：支持按班次、日期范围筛选历史交接记录
- 💾 **备份与恢复中心**：支持完整数据包的 JSON 导出和导入恢复
  - 导出内容包含：班次、角色、用户、检查项、事项、交接记录、恢复日志、复盘卡、版本号和导出时间
  - 导入前校验：结构校验、版本兼容检查、权限校验（仅配置管理员可恢复）
  - 恢复预览：展示导入包摘要、当前数据摘要、差异数量和冲突提示
  - 冲突检测：同名班次、同名用户、未关闭事项编号冲突
  - 恢复日志：完整记录每次恢复操作（成功/失败）、操作人、数据快照、冲突信息
- 🔍 **交接复盘**：对已关闭事项和交接记录生成复盘卡，追踪遗留风险和后续跟进
  - 班长可创建复盘卡，标记遗留风险、责任人、跟进截止时间和复盘结论
  - 普通值班员可查看复盘卡和补充跟进说明，但不能修改结论或删除复盘卡
  - 复盘卡自动带出原始摘要，所有修改写入可读日志
  - 复盘信息随导出/恢复持久化，刷新页面不丢失
- 🔍 **日常追踪**：班长可在复盘中心按条件快速筛选和标记重点关注复盘卡
  - 筛选条件：有遗留风险、无风险、只看我负责、已逾期
  - 重点关注标记：班长可将复盘卡标为重点关注，卡片和详情中稳定显示标记
  - 筛选条件和重点标记跟随 localStorage 保存，刷新页面不丢失
  - 完整 JSON 导出/恢复后重点标记和筛选条件完整带回
  - 恢复日志显示复盘卡数量对比，便于确认数据无异常变化
  - 普通值班员可查看筛选结果和重点标记，但不能修改重点状态
  - 班长修改重点状态时自动写入复盘卡操作日志
- 💾 **本地持久化**：使用 localStorage 存储，重启后数据不丢失

## 快速开始

### 环境要求

- 现代浏览器（Chrome、Firefox、Edge、Safari）
- 无需后端服务器，无需安装任何依赖

### 启动方式

1. 直接双击 `index.html` 文件，或在浏览器中打开该文件
2. 首次使用建议先创建样例数据（详见验收步骤）

## 项目结构

```
lfc-00007/
├── index.html          # 主页面
├── css/
│   └── style.css       # 样式文件
├── js/
│   ├── store.js        # 数据存储层（localStorage）
│   └── app.js          # 主应用逻辑（Vue 3）
└── README.md           # 本文件
```

## 数据模型

### 班次 (Shift)
- id: 唯一标识
- name: 班次名称
- startTime: 开始时间 (HH:mm)
- endTime: 结束时间 (HH:mm)

### 角色 (Role)
- id: 唯一标识
- name: 角色名称
- canCreate: 可创建事项
- canProcess: 可处理事项
- canClose: 可关闭事项
- canConfirm: 可接班确认

### 用户 (User)
- id: 唯一标识
- name: 用户姓名
- roleId: 所属角色ID

### 检查项 (CheckItem)
- id: 唯一标识
- name: 检查项名称
- required: 是否必填
- description: 检查说明

### 事项 (Item)
- id: 唯一标识
- title: 事项标题
- type: 类型（alert告警/change变更/other其他）
- description: 事项描述
- status: 状态（new/processing/pending_confirm/received/closed）
- version: 版本号（用于冲突检测）
- shiftId: 所属班次
- shiftDate: 班次日期
- creatorId/creatorName: 创建人
- assigneeId/assigneeName: 处理人
- createTime/updateTime/closeTime: 时间戳
- closeReason: 关闭原因
- history: 操作历史数组

### 交接记录 (HandoverRecord)
- id: 唯一标识
- shiftId/shiftName: 班次信息
- date: 交接日期
- handoverUserId/handoverName: 交班人
- takeoverUserId/takeoverName: 接班人
- handoverTime/takeoverTime: 时间
- status: 状态（pending/confirmed）
- itemIds/itemCount: 关联事项
- checkResults: 检查项结果
- itemsSnapshot: 事项快照

### 恢复日志 (RecoveryLog)
- id: 唯一标识
- action: 操作类型（数据恢复/导入失败）
- operatorId/operatorName: 操作人
- importVersion: 导入数据版本
- importTime: 导入数据导出时间
- beforeSummary: 恢复前数据摘要
- afterSummary: 恢复后数据摘要
- conflicts: 冲突信息（班次、用户、事项）
- reason: 失败原因（仅失败时）
- succeeded: 是否成功
- timestamp: 操作时间戳

## 验收步骤

### 第一步：创建样例数据

1. 打开 `index.html` 页面
2. 点击右上角「⚙️ 配置」按钮进入配置页
3. 点击左侧「当前班次」标签
4. 点击「🎯 创建样例数据（用于测试）」按钮
5. 在确认弹窗中点击「确定」
6. 系统会自动跳回看板页面
7. ✅ **预期结果**：页面顶部显示「白班」班次，看板上有3个样例事项

### 第二步：提交事项

1. 确保顶部当前用户选择了「张三（班长）」
2. 在看板页面点击「+ 新建事项」按钮
3. 填写事项信息：
   - 标题：测试告警事项
   - 类型：未处理告警
   - 描述：这是一个测试用的告警事项
   - 处理人：李四
4. 点击「创建事项」按钮
5. ✅ **预期结果**：
   - 「新建」列中出现新建的事项卡片
   - 显示事项标题、类型标签、处理人
   - 版本号为 v1

### 第三步：事项处理流转

1. 点击「新建」列中的「数据库CPU使用率告警」事项
2. 在详情弹窗中点击「🔄 开始处理」按钮
3. ✅ **预期结果**：
   - 事项状态变为「处理中」
   - 版本号从 v1 变为 v2
   - 操作历史中新增一条状态变更记录
   - 操作人显示为当前用户
4. 点击「📨 提交待确认」按钮
5. ✅ **预期结果**：
   - 事项状态变为「待接班确认」
   - 版本号再次递增
   - 操作历史中新增一条记录
6. 关闭详情弹窗，查看看板布局
7. ✅ **预期结果**：事项卡片移动到「待接班确认」列

### 第四步：补齐检查项并接班确认

1. 点击右上角「✅ 接班确认」按钮
2. 在弹出的确认窗口中：
   - 检查项列表中有5个检查项，其中4个标有「*必填」
   - 不勾选任何检查项，直接点击「确认接班」
3. ✅ **预期结果**：
   - 显示错误提示：「请先完成必填检查项：...」
   - 无法进行接班确认
4. 勾选所有必填检查项（告警平台巡检、服务器状态检查、网络设备状态、值班日志填写）
5. 点击「确认接班」按钮
6. ✅ **预期结果**：
   - 接班确认成功
   - 所有「待接班确认」的事项变为「已接收」状态
   - 历史记录中新增一条交接记录
   - 显示成功提示消息

### 第五步：权限校验测试

1. 将顶部当前用户切换为「赵六（观察员）」
2. 任意点击一个事项查看详情
3. ✅ **预期结果**：
   - 详情页底部没有编辑、处理、关闭等操作按钮
   - 只有「关闭」按钮关闭弹窗
4. 尝试点击「+ 新建事项」（在看板上）
5. ✅ **预期结果**：虽然可以打开弹窗，但创建时会提示「您没有创建事项的权限」
6. 切换为「李四（运维值班员）」
7. 点击一个「新建」状态的事项，尝试关闭
8. ✅ **预期结果**：没有关闭按钮，因为值班员没有关闭权限
9. 切换为「张三（班长）」
10. 点击一个事项，点击「❌ 关闭」按钮
11. 输入关闭原因，点击确认
12. ✅ **预期结果**：
    - 班长可以关闭事项
    - 事项状态变为「已关闭」
    - 关闭原因记录在详情中

### 第六步：版本冲突测试

> 注意：此测试需要在浏览器开发者工具中进行，或者通过快速连续操作模拟。

1. 选择「张三（班长）」作为当前用户
2. 打开一个「处理中」的事项详情，记住当前版本号（如 v3）
3. 打开浏览器开发者工具（F12），切换到 Console 标签
4. 执行以下命令手动模拟冲突场景：
   ```javascript
   const item = Store.Items.getAll()[0];
   console.log('当前版本:', item.version);
   const result = Store.Items.update(item.id, { title: item.title + ' (其他人修改)' }, {id:'user_li',name:'李四'}, item.version);
   console.log('李四修改结果:', result.success, '新版本:', result.item?.version);
   const result2 = Store.Items.update(item.id, { title: item.title + ' (张三修改)' }, {id:'user_zhang',name:'张三'}, item.version);
   console.log('张三修改结果(基于旧版本):', result2.success, '冲突:', result2.conflict);
   ```
5. ✅ **预期结果**：
   - 李四的修改成功，版本号递增
   - 张三基于旧版本的修改失败，返回 `conflict: true`
   - 张三的修改不会覆盖李四的修改
6. 刷新页面后再查看该事项
7. ✅ **预期结果**：事项标题是李四修改后的内容，没有被覆盖

### 第七步：导出交接单

1. 确保当前班次有事项和检查记录
2. 点击右上角「📤 导出交接单」按钮
3. ✅ **预期结果**：
   - 自动下载一个 `.txt` 文件
   - 文件名包含日期和班次名称
   - 文件内容包含：
     - 班次信息（班次、日期、时间、交班人、接班人）
     - 交接检查项列表（带勾选状态）
     - 按状态分组的事项列表
     - 操作历史记录

### 第八步：历史记录筛选

1. 点击顶部「📜 历史记录」按钮
2. ✅ **预期结果**：显示历史交接记录列表
3. 使用筛选器：
   - 选择班次为「白班」
   - 设置日期范围
   - 点击「筛选」按钮
4. ✅ **预期结果**：列表只显示符合条件的记录
5. 点击某条记录的「查看详情」按钮
6. ✅ **预期结果**：弹窗显示该交接单的详细信息
7. 点击「导出」按钮
8. ✅ **预期结果**：导出该条历史记录的交接单

### 第九步：数据持久化验证

1. 确认当前有一些事项和配置数据
2. 刷新浏览器页面（F5）
3. ✅ **预期结果**：
   - 所有事项状态保持不变
   - 事项版本号保持不变
   - 历史记录保持不变
   - 配置数据（班次、角色、用户、检查项）保持不变
4. 关闭浏览器，重新打开页面
5. ✅ **预期结果**：所有数据仍然存在

### 第十步：配置管理验证

1. 进入「配置」页面
2. 切换到「班次配置」标签
   - 添加一个新班次：名称「测试班」，时间「09:00-18:00」
   - 点击「保存班次配置」
   - ✅ 班次列表出现新班次
3. 切换到「角色配置」标签
   - 添加一个新角色：名称「测试角色」，勾选「可创建事项」
   - 点击「保存角色配置」
   - ✅ 角色列表出现新角色
4. 切换到「用户配置」标签
   - 添加一个新用户：姓名「测试用户」，选择刚才创建的角色
   - 点击「保存用户配置」
   - ✅ 用户列表出现新用户
5. 切换到「交接模板」标签
   - 添加一个新检查项：名称「测试检查项」，勾选「必填」
   - 点击「保存检查模板」
   - ✅ 检查项列表出现新检查项
6. 切换到「当前班次」标签
   - 选择刚才创建的「测试班」作为当前班次
   - 保存设置
   - ✅ 顶部显示当前班次为「测试班」

### 第十一步：备份与恢复中心 - 成功导出

1. 确保已创建样例数据，顶部当前用户选择为「张三（班长）」
2. 点击顶部导航栏的「💾 备份恢复」按钮进入备份与恢复中心
3. ✅ **预期结果**：
   - 页面标题显示「💾 备份与恢复中心」
   - 标题右侧显示当前数据版本号（当前版本：1.1）
   - 页面分为三个区域：「📤 导出数据包」「📥 从本地导入恢复」「📋 恢复日志」
   - 「📤 导出数据包」区域内：
     - 说明文字：「导出完整的数据包，包含班次、角色、用户、检查项、事项、交接记录、恢复日志、版本号和导出时间。」
     - 有数据统计卡片，显示班次、角色、用户、检查项、事项、交接记录、恢复日志的当前数量
     - 有一个绿色按钮：「📥 导出完整数据包」
   - 「📥 从本地导入恢复」区域内：
     - 说明文字：「选择本地 JSON 文件进行数据恢复。导入前将进行结构校验、版本兼容检查和权限校验。」
     - 有一个可点击的文件选择区域：「📁 点击选择 JSON 文件」
   - 「📋 恢复日志」区域内：
     - 显示「暂无恢复日志」（首次使用时）
4. 点击「📥 导出完整数据包」按钮
5. ✅ **预期结果**：
   - 浏览器自动下载一个 `.json` 文件
   - 文件名格式为 `值班交接备份_YYYY-MM-DD.json`
   - 页面显示「数据包已导出」成功提示
6. 打开下载的 JSON 文件，验证内容包含以下字段：
   - ✅ `shifts`（班次数组）
   - ✅ `roles`（角色数组）
   - ✅ `users`（用户数组）
   - ✅ `checkItems`（检查项数组）
   - ✅ `items`（事项数组）
   - ✅ `handoverRecords`（交接记录数组）
   - ✅ `recoveryLogs`（恢复日志数组）
   - ✅ `currentShift`（当前班次设置对象）
   - ✅ `version`（版本号，值为 `"1.1"`）
   - ✅ `exportTime`（导出时间，ISO 格式时间戳）

### 第十二步：备份与恢复中心 - 非法 JSON 校验

1. 准备一个内容为乱码或格式错误的文件，命名为 `invalid.json`
2. 在备份恢复页面的「📥 从本地导入恢复」区域，点击「📁 点击选择 JSON 文件」，选择刚准备的非法文件
3. ✅ **预期结果**：
   - 文件选择区域显示已选择的文件名
   - 出现两个按钮：「清除」和「🔍 预览并验证」
   - 页面下方显示红色错误提示，内容包含「JSON 格式解析失败」
4. 准备一个缺少必要字段的文件，内容为 `{ "missing": "fields" }`，命名为 `invalid2.json`
5. 点击「清除」按钮清空选择，重新选择 `invalid2.json`，然后点击「🔍 预览并验证」按钮
6. ✅ **预期结果**：
   - 不会弹出导入预览窗口
   - 页面下方显示红色错误提示，内容包含「数据结构校验失败」和「缺少必要字段」

### 第十三步：备份与恢复中心 - 权限不足校验

1. 将顶部当前用户切换为「李四（运维值班员）」或「赵六（观察员）」
2. 在「📥 从本地导入恢复」区域，选择第十一步导出的有效 JSON 备份文件
3. 点击「🔍 预览并验证」按钮
4. ✅ **预期结果**：
   - 弹出「🔍 导入预览与确认」弹窗
   - 弹窗内「版本检查」显示绿色通过：「版本检查：版本兼容（导入版本：1.1，当前版本：1.1）」
   - 弹窗内「权限检查」显示红色警告：「权限检查：当前用户没有配置管理权限，无法执行恢复操作」
   - 弹窗底部右侧的「⚠️ 确认覆盖恢复」按钮处于禁用状态（灰色，不可点击）
   - 弹窗底部左侧有「取消」按钮
5. 点击「取消」按钮关闭弹窗
6. 将顶部当前用户切换回「张三（班长）」
7. 重新选择文件并点击「🔍 预览并验证」
8. ✅ **预期结果**：
   - 「权限检查」显示绿色通过：「权限检查：权限验证通过」
   - 「⚠️ 确认覆盖恢复」按钮可用

### 第十四步：备份与恢复中心 - 无冲突恢复验证

1. 确保顶部当前用户为「张三（班长）」
2. 在「📥 从本地导入恢复」区域，选择第十一步导出的备份文件（与当前数据完全一致，无任何冲突）
3. 点击「🔍 预览并验证」按钮
4. ✅ **预期结果**：
   - 弹出「🔍 导入预览与确认」弹窗
   - 版本检查、权限检查均为绿色通过
   - 「📊 数据摘要对比」表格显示：
     - 列标题为「数据类型」「导入包」「当前数据」「差异」
     - 所有数据类型的「导入包」和「当前数据」数量完全一致
     - 「差异」列均为 0，无绿色或红色高亮
     - 表格下方显示「共有 0 类数据将发生变化」
   - **不出现** 黄色的「⚠️ 检测到以下冲突」警告区域（因为没有冲突）
   - 弹窗底部显示「导入包信息」，包含导出时间和导出版本
5. 点击「⚠️ 确认覆盖恢复」按钮
6. ✅ **预期结果**：
   - 弹出二次确认框：「确定要覆盖当前所有数据吗？此操作不可撤销！」
7. 点击「确定」
8. ✅ **预期结果**：
   - 弹窗关闭
   - 页面显示「数据恢复成功」提示
   - 页面自动刷新
   - 查看「📋 恢复日志」区域，出现一条新记录
   - 日志卡片左侧绿色竖条，状态标记为 ✅，操作类型为「数据恢复」，显示操作时间
   - 展开日志详情，验证：
     - ✅ 操作人：张三
     - ✅ 导入版本：1.1
     - ✅ 有数据数量对比表格（列标题为「类型」「恢复前」「恢复后」），恢复前后数量一致
     - ✅ **没有** 黄色的「⚠️ 冲突提示」区域（因为无冲突）
9. 刷新浏览器页面（F5）
10. ✅ **预期结果**：
    - 所有配置、事项状态、历史记录、版本号都保持不变
    - 「📋 恢复日志」区域中的那条成功日志仍然存在

### 第十五步：备份与恢复中心 - 真实冲突检测与取消恢复

1. 先导出一份当前数据作为备份 A
2. 点击「⚙️ 配置」进入配置页面
3. 在「班次配置」标签页，添加一个新班次「测试冲突班」，时间任意，保存
4. 在「用户配置」标签页，添加一个新用户「测试冲突用户」，角色任意，保存
5. 点击「💾 备份恢复」回到备份恢复中心，导出当前数据作为备份 B（包含新增的「测试冲突班」和「测试冲突用户」）
6. 再点击「⚙️ 配置」回到配置页面
7. 在「班次配置」标签页，删除「测试冲突班」并保存
8. 在「用户配置」标签页，删除「测试冲突用户」并保存
9. 回到「💾 备份恢复」页面，在导入区域选择步骤 5 导出的备份 B
10. 点击「🔍 预览并验证」
11. ✅ **预期结果**：
    - 弹出「🔍 导入预览与确认」弹窗
    - 「📊 数据摘要对比」表格中，班次和用户的「差异」列显示 +1（绿色高亮）
    - 表格下方显示「共有 2 类数据将发生变化」
    - 出现黄色的「⚠️ 检测到以下冲突」警告区域，包含：
      - ✅ 「同名班次：测试冲突班」
      - ✅ 「同名用户：测试冲突用户」
      - ✅ 提示文字：「恢复后以上数据将被导入包中的数据覆盖。」
12. 点击「取消」按钮（或点击弹窗外灰色区域）
13. ✅ **预期结果**：
    - 弹窗关闭，不执行任何数据变更
    - 点击「⚙️ 配置」→「班次配置」，「测试冲突班」仍不存在
    - 点击「⚙️ 配置」→「用户配置」，「测试冲突用户」仍不存在
    - 回到「💾 备份恢复」页面，「📋 恢复日志」区域没有新增记录
14. 重新选择备份 B，点击「🔍 预览并验证」→「⚠️ 确认覆盖恢复」→「确定」，执行恢复
15. ✅ **预期结果**：
    - 恢复成功后查看「📋 恢复日志」区域
    - 最新的日志详情中黄色「⚠️ 冲突提示」区域清楚显示：
      - 同名班次：测试冲突班
      - 同名用户：测试冲突用户
    - 值班人员可以一目了然地看到这次恢复存在的数据覆盖风险

### 第十六步：运行自动化测试

1. 打开命令行终端，进入项目根目录
2. 运行命令：`node test.js`
3. ✅ **预期结果**：
   - 终端依次显示各模块测试结果
   - 包括「--- 8. 交接复盘测试 ---」部分
   - 所有测试用例均显示 ✅ 通过
   - 最终输出「🎉 所有测试通过！」
4. 交接复盘相关测试覆盖以下场景：
   - ✅ 创建复盘卡 - 从已关闭事项
   - ✅ 创建复盘卡 - 从交接记录
   - ✅ 复盘卡权限不足 - 值班员不能修改结论
   - ✅ 复盘卡权限不足 - 值班员不能删除
   - ✅ 复盘卡权限不足 - 观察员不能修改结论
   - ✅ 修改复盘卡留痕 - 班长修改结论写入日志
   - ✅ 普通值班员可以添加跟进说明
   - ✅ 班长删除复盘卡
   - ✅ 同一来源不能重复创建复盘卡
   - ✅ 复盘卡权限校验方法
   - ✅ 复盘卡导出包含在完整数据包中
   - ✅ 复盘卡导出恢复后持久化
   - ✅ 复盘卡校验 - reviewCards 字段应在导入校验中

### 第十七步：创建复盘卡

1. 确保已创建样例数据，并完成了事项流转和接班确认（有已关闭的事项）
2. 选择「张三（班长）」作为当前用户
3. 在看板页面，找到「已关闭」列中的事项卡片
4. ✅ **预期结果**：已关闭事项卡片底部显示「+ 创建复盘」按钮
5. 点击「+ 创建复盘」按钮
6. ✅ **预期结果**：弹出「🔍 创建复盘卡」弹窗
7. 弹窗中「原始摘要」自动带出事项信息（包含事项类型、标题、关闭原因）
8. 勾选「标记为有遗留风险」
9. 输入风险描述：「需要持续监控」
10. 选择责任人为「李四」
11. 设置跟进截止时间为一个未来日期
12. 输入复盘结论：「已确认处理完成，需持续关注」
13. 点击「创建复盘卡」按钮
14. ✅ **预期结果**：
    - 提示「复盘卡已创建」
    - 自动打开复盘卡详情弹窗
    - 详情中显示原始摘要、风险标记、责任人、截止时间、复盘结论
15. 关闭详情弹窗，查看已关闭列中的事项卡片
16. ✅ **预期结果**：卡片底部显示「🔍 已复盘」绿色标签

### 第十八步：复盘卡权限校验

1. 将当前用户切换为「李四（运维值班员）」
2. 点击已关闭列中的「🔍 已复盘」标签
3. ✅ **预期结果**：
    - 打开复盘卡详情弹窗
    - 弹窗底部没有「✏️ 编辑复盘」按钮
    - 弹窗底部没有「🗑️ 删除复盘卡」按钮
    - 只有「关闭」按钮和跟进说明输入框
4. 在跟进说明输入框中输入「已确认磁盘空间恢复」
5. 点击「添加跟进说明」按钮
6. ✅ **预期结果**：
    - 提示「跟进说明已添加」
    - 跟进说明列表中出现一条记录，操作人为李四
7. 将当前用户切换回「张三（班长）」
8. 再次查看复盘卡详情
9. ✅ **预期结果**：
    - 有「✏️ 编辑复盘」和「🗑️ 删除复盘卡」按钮
    - 跟进说明列表中有李四添加的记录

### 第十九步：修改复盘卡留痕验证

1. 以班长身份打开复盘卡详情
2. 点击「✏️ 编辑复盘」按钮
3. ✅ **预期结果**：切换为编辑模式，各字段可编辑
4. 修改复盘结论为「风险已消除」
5. 取消勾选「标记为有遗留风险」
6. 点击「保存修改」按钮
7. ✅ **预期结果**：
    - 提示「复盘信息已更新」
    - 回到查看模式，风险标记显示「✅ 否」
    - 结论已更新
8. 查看「操作日志」区域
9. ✅ **预期结果**：
    - 有「修改复盘结论」日志条目
    - 日志显示操作人、操作时间
    - 日志详情中包含「遗留风险」和「复盘结论已更新」变更描述

### 第二十步：复盘卡持久化与导出恢复验证

1. 刷新浏览器页面（F5）
2. 点击顶部导航栏「🔍 交接复盘」按钮
3. ✅ **预期结果**：
    - 复盘中心显示之前创建的复盘卡
    - 复盘卡显示风险标记、责任人、截止时间等信息
4. 点击复盘卡查看详情
5. ✅ **预期结果**：复盘状态、责任人、截止时间、跟进说明、操作日志顺序都保持不变
6. 进入「💾 备份恢复」页面，点击「📥 导出完整数据包」
7. 用文本编辑器打开导出的 JSON 文件
8. ✅ **预期结果**：JSON 中包含 `reviewCards` 数组，数组中的复盘卡数据完整
9. 在看板页面删除或修改一些数据
10. 回到「💾 备份恢复」页面，选择刚才导出的文件并执行恢复
11. ✅ **预期结果**：恢复成功后，进入「🔍 交接复盘」页面，复盘卡数据完整恢复

### 第二十一步：复盘卡重复创建防重验证（必看）

**准备工作**：确保当前以「张三（班长）」登录，且看板「已关闭」列中至少有一个**没有复盘标签**的事项。如果已有复盘卡，可以先在复盘详情中删除，或关闭一个新的事项用于测试。

---

**场景 1：已关闭事项的前端入口不会出现重复创建入口**

1. 在看板中找到一个**已关闭**的事项
2. ✅ **预期结果**：卡片底部的状态是二选一，不是同时出现两个按钮：
   - 如果还没复盘：只显示 **「+ 创建复盘」** 黄色按钮
   - 如果已经复盘：只显示 **「🔍 已复盘」** 绿色标签
3. 不存在两种入口同时出现的情况

---

**场景 2：创建后再次点击同一条事项的入口 - 前端拦截**

1. 确认要测试的事项当前显示「+ 创建复盘」按钮
2. 点击「+ 创建复盘」，弹出「🔍 创建复盘卡」弹窗
3. 填写表单（比如勾选遗留风险，选责任人，写一段结论）
4. 点击「创建复盘卡」按钮
5. ✅ **预期结果**：
   - 提示绿色成功 Toast：「复盘卡已创建」
   - 自动关闭创建弹窗并打开复盘卡详情
   - 关闭详情弹窗后，事项卡片底部从「+ 创建复盘」变成「🔍 已复盘」标签
6. **关键验证**：再一次点击该事项的「🔍 已复盘」标签（注意：不要再通过其他途径点创建入口）
7. ✅ **预期结果**：只会打开**已有**复盘卡的详情，不会出现新的创建弹窗

---

**场景 3：Store 层幂等拦截（极端情况兜底，即使前端漏了也防重）**

此步骤在浏览器的开发者工具 Console 中执行，模拟前端由于网络延迟、快速连击、并发标签页等造成的重复调用：

1. 找到刚才已复盘的事项 ID（可在 Console 执行 `Store.Items.getAll().find(i=>i.status==='closed').id` 查看，记下来）
2. 执行以下代码（把 `'事项ID'` 换成刚才查到的真实 ID）：

```javascript
const adminUser = Store.Users.getById('user_zhang');

// 第 1 次调用 - 本来就有一张卡
const r1 = Store.ReviewCards.create({
  sourceType: 'closed_item',
  sourceId: '事项ID',
  sourceSummary: '试图重复创建',
  conclusion: '试图覆盖原有结论'
}, adminUser);

// 第 2 次调用 - 间隔 0ms，模拟快速连击
const r2 = Store.ReviewCards.create({
  sourceType: 'closed_item',
  sourceId: '事项ID',
  sourceSummary: '再次试图覆盖',
  conclusion: '结论2'
}, adminUser);

console.log('第1次结果：', { created: r1.created, sameId: true /* 原本就是首次或复用 */, count: Store.ReviewCards.getAll().length });
console.log('第2次结果：', { created: r2.created, message: r2.message, sameIdAsFirst: r2.card.id === r1.card.id, count: Store.ReviewCards.getAll().length });
```

3. ✅ **预期结果**（观察 Console 输出）：
   - `r1.created === false`（因为该事项原本已复盘，首次被调 create 也应拦截）
   - `r2.created === false`
   - `r1.card.id === r2.card.id`（两次都返回同一张卡的 id，不生成新卡）
   - `Store.ReviewCards.getAll().filter(c => c.sourceId === '事项ID').length === 1`（同一个 sourceId 下始终只有 1 张）

---

**场景 4：导出 JSON 不包含重复复盘卡**

1. 保持场景 3 执行后的状态
2. 进入「💾 备份恢复」页面，点击「📥 导出完整数据包」下载 JSON 文件
3. 用文本编辑器打开 JSON 文件，搜索 `reviewCards` 数组
4. ✅ **预期结果**：
   - `reviewCards` 数组中，同一个 `sourceId` 最多出现一次
   - 不会出现两张复盘卡的 `sourceId` 相同的情况
   - 如果发现重复，刷新页面后再导出一次会自动清理（`getAll()` 在读取时发现重复会自动修掉）

---

**场景 5：导入有重复 sourceId 的 JSON 后去重，并继续创建仍不重复**

此步骤验证「老版本产生重复数据 → 新版本恢复 → 自动去重」的兼容性：

1. 用文本编辑器打开一个已导出的 JSON 文件，复制 `reviewCards` 数组中的某一个对象，改一下 `id`，`sourceId` **保持相同**，塞回数组末尾，保存文件
2. 回到页面，进入「💾 备份恢复」页面
3. 选择刚才修改过的 JSON 文件，点击「🔍 预览并验证」
4. ✅ **预期结果**：预览可以正常通过，不会因为有重复而报错
5. 执行「确认恢复」
6. 恢复成功后，进入「🔍 交接复盘」中心
7. ✅ **预期结果**：复盘卡列表中，同一个 sourceId 只保留 1 张（保留 createTime 更早的那张），不会出现两张相同来源的卡
8. 回到看板，针对相同来源的事项，通过正常入口点击「🔍 已复盘」查看详情
9. 再在 Console 中执行一次场景 3 中的 `ReviewCards.create` 代码
10. ✅ **预期结果**：`created === false`，`card.id` 仍等于之前的卡 id，不新增

---

**场景 6：权限控制在修复后未被破坏（回归检查）**

1. 切换到「李四（运维值班员）」
2. 找到已复盘的事项，点击「🔍 已复盘」打开详情
3. ✅ **预期结果**：
   - 详情弹窗底部只有「添加跟进说明」和「关闭」两个按钮
   - **没有**「✏️ 编辑复盘」按钮
   - **没有**「🗑️ 删除复盘卡」按钮
4. 在跟进说明文本框中输入一段文字，点击「添加跟进说明」
5. ✅ **预期结果**：提示成功，跟进说明列表和操作日志中新增对应的记录（李四可以补充跟进说明，权限未被修复破坏）
6. 切换回「张三（班长）」再次打开详情
7. ✅ **预期结果**：可以看到「✏️ 编辑复盘」和「🗑️ 删除复盘卡」按钮，班长权限正常

### 第二十二步：日常追踪 - 复盘中心筛选功能

1. 确保已创建样例数据，且有复盘卡（至少需要2张，一张有风险、一张无风险）
2. 选择「张三（班长）」作为当前用户
3. 点击顶部导航栏「🔍 交接复盘」进入复盘中心
4. ✅ **预期结果**：
   - 页面顶部显示筛选栏，包含4个筛选条件复选框：「⚠️ 有遗留风险」「✅ 无风险」「👤 只看我负责」「⏰ 已逾期」
   - 还有一个「重置筛选」按钮
   - 标题右侧显示「共 X 张复盘卡，当前显示 Y 张」
5. 勾选「⚠️ 有遗留风险」
6. ✅ **预期结果**：列表只显示有遗留风险的复盘卡
7. 取消勾选，勾选「✅ 无风险」
8. ✅ **预期结果**：列表只显示无遗留风险的复盘卡
9. 勾选「👤 只看我负责」
10. ✅ **预期结果**：列表只显示当前用户（张三）为责任人的复盘卡
11. 勾选「⏰ 已逾期」
12. ✅ **预期结果**：列表只显示有遗留风险且截止时间已过的复盘卡
13. 点击「重置筛选」按钮
14. ✅ **预期结果**：所有筛选条件重置，显示全部复盘卡

### 第二十三步：日常追踪 - 重点关注标记

1. 以班长身份进入复盘中心
2. 找到一张复盘卡，点击卡片右下角的「☆ 标为重点」按钮
3. ✅ **预期结果**：
   - 按钮变为「⭐ 取消重点」（橙色）
   - 卡片左侧边框变为橙色
   - 卡片背景变为浅黄色
   - 卡片标题旁出现「⭐ 重点关注」标签
4. 点击该复盘卡查看详情
5. ✅ **预期结果**：
   - 详情弹窗标题旁显示「⭐ 重点关注」标签
   - 详情中「重点标记」行显示「⭐ 重点关注」
   - 弹窗底部有「⭐ 取消重点」按钮（橙色）
6. 查看「操作日志」区域
7. ✅ **预期结果**：
   - 有「标记为重点关注」日志条目
   - 日志显示操作人（张三）、操作时间
   - 日志详情包含标记信息
8. 点击「⭐ 取消重点」按钮
9. ✅ **预期结果**：
   - 按钮变为「☆ 标为重点」
   - 操作日志新增「取消重点关注」记录

### 第二十四步：日常追踪 - 权限控制验证

1. 将当前用户切换为「李四（运维值班员）」
2. 进入复盘中心
3. ✅ **预期结果**：
   - 可以看到筛选栏和筛选功能正常使用
   - 可以看到复盘卡上的重点标记（⭐ 重点关注标签）
   - 复盘卡底部**没有**「☆ 标为重点」/「⭐ 取消重点」按钮
4. 点击复盘卡查看详情
5. ✅ **预期结果**：
   - 详情中可以看到「重点标记」行
   - 详情弹窗底部**没有**「☆ 标为重点」/「⭐ 取消重点」按钮
   - **没有**「✏️ 编辑复盘」和「🗑️ 删除复盘卡」按钮

### 第二十五步：日常追踪 - 筛选条件持久化验证

1. 以班长身份进入复盘中心
2. 勾选「⚠️ 有遗留风险」筛选条件
3. 刷新浏览器页面（F5）
4. ✅ **预期结果**：
   - 页面重新加载后，「⚠️ 有遗留风险」筛选条件仍为勾选状态
   - 复盘卡列表显示筛选后的结果
5. 点击「重置筛选」按钮
6. 刷新浏览器页面
7. ✅ **预期结果**：所有筛选条件为未勾选状态

### 第二十六步：日常追踪 - 重点关注标记持久化验证

1. 以班长身份进入复盘中心
2. 将某张复盘卡标记为重点关注
3. 刷新浏览器页面（F5）
4. ✅ **预期结果**：
   - 该复盘卡仍然显示重点标记（⭐ 重点关注标签、橙色边框、浅黄背景）
   - 点击查看详情，重点标记仍存在

### 第二十七步：日常追踪 - 导出恢复验证

1. 确保复盘卡有重点标记
2. 进入「💾 备份恢复」页面，点击「📥 导出完整数据包」下载 JSON 文件
3. 用文本编辑器打开导出的 JSON 文件
4. ✅ **预期结果**：
   - `reviewCards` 数组中复盘卡的 `focusMark` 字段正确反映重点标记状态
5. 回到复盘中心，取消某张复盘卡的重点标记
6. 进入「💾 备份恢复」页面，选择刚才导出的文件执行恢复
7. ✅ **预期结果**：
   - 恢复成功后，复盘卡的重点标记恢复为导出时的状态
8. 查看「📋 恢复日志」
9. ✅ **预期结果**：
   - 日志详情中的数据对比表格包含「复盘卡」行
   - 恢复前/后的复盘卡数量一致，无异常变化

### 第二十八步：日常追踪 - 自动化测试验证

1. 打开命令行终端，进入项目根目录
2. 运行命令：`node test.js`
3. ✅ **预期结果**：
   - 终端显示「--- 9. 日常追踪能力测试 ---」部分
   - 所有新增测试用例均显示 ✅ 通过，包括：
     - ✅ focusMark 字段 - 创建复盘卡时默认为 false
     - ✅ focusMark 字段 - 旧数据缺少 focusMark 时自动补齐
     - ✅ 班长可以标记重点关注
     - ✅ 班长可以取消重点关注
     - ✅ 值班员不能修改重点标记
     - ✅ 观察员不能修改重点标记
     - ✅ 重点关注标记随导出/恢复持久化
     - ✅ 恢复日志包含复盘卡数量对比
     - ✅ 筛选条件 localStorage 持久化
     - ✅ 筛选逻辑 - 有遗留风险
     - ✅ 筛选逻辑 - 已逾期
     - ✅ 筛选逻辑 - 只看我负责
     - ✅ 重点关注标记写入操作日志
     - ✅ 焦点标记与导出恢复 - 恢复后日志能看出复盘卡数量无异常变化

### 第二十九步：筛选配置 - 导出字段验证

1. 以班长身份进入复盘中心
2. 在筛选区域勾选「⚠️ 有遗留风险」和「⏰ 已逾期」
3. 进入「💾 备份恢复」页面，点击「📥 导出完整数据包」下载 JSON 文件
4. 用文本编辑器打开导出的 JSON 文件
5. ✅ **预期结果**：
   - 文件顶层有 `reviewFilters` 字段
   - `reviewFilters` 包含 `hasRisk`、`noRisk`、`myResponsible`、`overdue` 四个布尔字段
   - `hasRisk` 为 `true`、`overdue` 为 `true`，与页面勾选状态一致

### 第三十步：筛选配置 - 导入预览与覆盖提示

1. 继续上面的测试，先在页面上取消所有筛选（全部为未勾选状态）
2. 进入「💾 备份恢复」页面，选择刚才导出的 JSON 文件
3. 点击「📋 预览恢复内容」
4. ✅ **预期结果**：
   - 预览弹窗中出现「🔍 复盘筛选配置」独立区块
   - 区块中显示筛选条件对比表格，包含「筛选条件」「导入包中」「当前本地」「状态」四列
   - 「有遗留风险」和「已逾期」两行的状态显示为"将被覆盖"或类似提示
   - 冲突提示区域包含「复盘筛选配置变化」条目

### 第三十一步：筛选配置 - 恢复后持久化验证

1. 在预览弹窗中点击「✅ 确认恢复」执行恢复
2. 恢复成功后切换到「🔍 复盘中心」标签页
3. ✅ **预期结果**：
   - 筛选区域的「⚠️ 有遗留风险」和「⏰ 已逾期」为勾选状态
   - 复盘卡列表显示筛选后的结果
4. 刷新浏览器页面（F5）
5. ✅ **预期结果**：
   - 页面重新加载后，筛选条件仍然保持勾选状态
   - 刷新不影响筛选配置

### 第三十二步：筛选配置 - 旧包兼容验证

1. 用文本编辑器打开之前导出的 JSON 文件
2. 删除文件中的 `reviewFilters` 字段（模拟旧版本导出包）
3. 保存文件
4. 先在页面上将所有筛选条件重置为未勾选
5. 进入「💾 备份恢复」页面，选择修改后的 JSON 文件
6. 点击「📋 预览恢复内容」
7. ✅ **预期结果**：
   - 预览弹窗的「🔍 复盘筛选配置」区块显示"导入包中不包含复盘筛选配置，恢复后筛选条件将保持当前设置不变"
   - 没有筛选配置冲突提示
8. 执行恢复
9. ✅ **预期结果**：
   - 恢复成功后，筛选条件保持未勾选状态（没有被修改）

### 第三十三步：筛选配置 - 恢复日志验证

1. 恢复执行完成后，进入「📋 恢复日志」页面
2. 点击最新一条恢复日志的「查看详情」
3. ✅ **预期结果**：
   - 日志详情中出现「🔍 复盘筛选配置」条目
   - 如果导入包有筛选配置且与本地不同，显示"已恢复，变化项：xxx"
   - 如果导入包没有筛选配置，显示"导入包中无筛选配置，保持本地设置不变"
   - 清晰说明筛选配置是否被覆盖

### 第三十四步：筛选配置 - 自动化测试验证

1. 打开命令行终端，进入项目根目录
2. 运行命令：`node test.js`
3. ✅ **预期结果**：
   - 终端显示「--- 10. 复盘筛选配置一等公民 - 导出导入测试 ---」部分
   - 所有新增测试用例均显示 ✅ 通过，包括：
     - ✅ 导出数据包含 reviewFilters 字段
     - ✅ 筛选配置校验函数 - validateReviewFilters
     - ✅ 筛选配置迁移函数 - migrateReviewFilters
     - ✅ 旧包兼容 - 导入包没有 reviewFilters 时使用本地设置
     - ✅ 筛选配置冲突检测 - 本地与导入包不同时检测为冲突
     - ✅ 筛选配置冲突检测 - 本地与导入包相同时不检测为冲突
     - ✅ 筛选配置导入恢复 - 恢复后 localStorage 中正确写入
     - ✅ 筛选配置非法字段 - 导入时自动迁移并使用默认值
     - ✅ 权限控制 - 值班员无法通过恢复修改筛选配置
     - ✅ 预览导入 - 返回值包含筛选配置详细信息
     - ✅ 筛选配置恢复后持久化 - 模拟刷新页面后仍生效

### 35. 导出异常交接包（字段完整性）
1. 顶部切换当前用户为「张三（班长）」，当前班次选择「白班」，当前日期使用默认值
2. 点击顶部导航栏的「📦 异常交接包」按钮
3. ✅ **预期结果**：
   - 弹出导出弹窗，显示交班班次（白班）、交班人（张三）
   - 「选择要交接的事项」区域列出当前班次所有事项，默认全部勾选
   - 「交接检查项」区域列出所有检查项，可勾选
   - 「接班候选人」区域列出所有具备接班确认权限的用户（班长、运维值班员），可多选
4. 勾选 2 个事项，勾选 1-2 个检查项，选择 1 位接班候选人（如李四）
5. 点击「📦 导出交接包」按钮
6. ✅ **预期结果**：浏览器下载一个 JSON 文件，文件名格式为 `exception-handover-YYYYMMDD-班次名.json`
7. 用文本编辑器打开下载的 JSON 文件
8. ✅ **预期结果**：文件包含以下字段且值正确：
   - `packageType`: 值为 `"exception_handover"`
   - `packageVersion`: 值为 `"1.0"`
   - `exportTime`: ISO 格式的导出时间
   - `shiftInfo`: 包含 `shiftId`、`shiftName`（白班）、`date`
   - `handoverUser`: 包含交班人的 `id`、`name`、`roleId`、`roleName`
   - `operator`: 包含当前操作员信息
   - `takeoverCandidates`: 数组，包含刚才选中的候选人信息
   - `checkResults`: 数组，每个检查项包含 `id`、`name`、`required`、`checked`
   - `items`: 数组，每项为完整的事项快照（含 `id`、`title`、`version`、`history` 等所有字段）

### 36. 导入异常交接包 - 预览四分类（新增/覆盖/冲突/跳过）
1. 先在「配置 → 当前班次」切换到「中班」，创建 1 个新事项（如「测试事项A」），保持为「新建」状态
2. 打开另一个浏览器窗口（或在同一窗口），切换回「白班」，创建 3 个事项：
   - 事项 B（新建状态，后续作为「新增」导入到中班）
   - 事项 C（新建状态，内容与步骤 1 中的「测试事项A」不同，后续作为「覆盖」）
   - 事项 D（新建状态，后续作为「跳过」或「冲突」）
3. 在「白班」窗口按步骤 35 导出包含事项 B、C、D 的异常交接包
4. 回到「中班」窗口，修改「测试事项A」的标题或描述，保存后关闭详情
5. 点击顶部「💾 备份恢复」tab，找到「📦 导入异常交接包」区域
6. 点击文件选择框，选择步骤 3 导出的 JSON 文件
7. 点击「🔍 预览交接内容」按钮
8. ✅ **预期结果**：弹出预览窗口，显示：
   - **包信息**：交班班次、交班人、接班候选人（如有）、导出时间、包版本
   - **版本检查**：显示版本兼容提示
   - **权限检查**：显示当前用户是否有权限导入
   - **事项变更预览**：顶部有彩色标签显示新增 X、覆盖 X、冲突 X、跳过 X 的数量
   - 下方表格中每一行有颜色区分：
     - 🆕 **新增（绿色）**：本地不存在的事项
     - 🔄 **覆盖（蓝色）**：本地存在但内容有差异，且版本/更新时间不旧于导入包
     - ⚠️ **冲突（红色）**：本地版本更新或更新时间更晚
     - ⏭️ **跳过（灰色）**：本地内容与导入内容完全一致
   - 每行显示：事项名称、导入状态、导入版本、本地版本、详细说明
   - 「检查项结果」区域显示包中包含的检查项勾选状态

### 37. 冲突处理场景 - 4 类情况的明确提示
**场景 1：包字段缺失**
1. 用文本编辑器编辑一份导出的交接包 JSON，删除 `shiftInfo` 字段
2. 在「导入异常交接包」区域上传修改后的文件，点击「🔍 预览交接内容」
3. ✅ **预期结果**：预览无法打开，导入区域下方显示红色错误提示，明确列出缺少的字段

**场景 2：版本不同（本地版本更新）**
1. 导出一个包含某事项的交接包
2. 在看板中修改该事项（如改标题），保存后版本号会递增
3. 导入刚才导出的旧交接包，预览该事项
4. ✅ **预期结果**：该事项被标记为 ⚠️ **冲突**，说明中显示「本地版本 vX 比导入版本 vY 更新」，且不会被勾选为可导入

**场景 3：本地已有更新（更新时间更晚）**
1. 导出交接包
2. 修改事项后立即撤销（或任何触发 updateTime 变化的操作）
3. 导入刚才的包
4. ✅ **预期结果**：如本地更新时间晚于导入包的 updateTime，会被标记为冲突，理由中明确提及更新时间

**场景 4：用户无接班权限**
1. 顶部切换当前用户为「赵六（观察员）」
2. 尝试预览和导入一份异常交接包
3. ✅ **预期结果**：
   - 预览窗口中「权限检查」显示红色错误：「当前用户角色无接班确认权限」
   - 底部「✅ 确认导入」按钮呈灰色禁用状态，无法点击

### 38. 可回退处理（取消不执行）
1. 选择一份有效的异常交接包并打开预览窗口
2. 观察预览结果（如有新增/覆盖事项）
3. 点击「取消」按钮或点击弹窗外部关闭
4. 返回看板查看事项列表
5. ✅ **预期结果**：本地事项没有任何变化，未新增、未覆盖任何内容

### 39. 确认导入 - 状态接续 + 操作日志 + 刷新后生效
1. 在预览窗口中确认有若干「新增」和「覆盖」事项（确保权限检查通过）
2. 记下当前看板中事项的数量和覆盖事项的当前内容
3. 点击「✅ 确认导入（X项）」按钮
4. ✅ **预期结果**：
   - 弹窗关闭，页面自动刷新看板数据
   - 看板上出现新增的事项（状态与包中一致）
   - 覆盖的事项内容变为导入包中的内容
   - 覆盖事项的版本号比之前递增
   - 顶部弹出成功提示：说明新增 X 项、覆盖 X 项、冲突 X 项、跳过 X 项
5. 打开任一被覆盖事项的详情，查看「历史记录」
6. ✅ **预期结果**：历史记录最后一条显示「异常交接包覆盖更新」操作，记录操作人、时间和版本变化
7. 点击顶部「💾 备份恢复」tab，查看「恢复日志」区域
8. ✅ **预期结果**：列表中出现一条新日志：
   - 操作类型为「异常交接包导入」
   - 显示操作人、操作时间
   - 包含包信息（交班班次、交班人、导出时间）
   - 显示导入的事项 ID 列表、跳过数量、冲突数量
9. 按浏览器刷新键（F5 / Ctrl+R）重新加载页面
10. ✅ **预期结果**：刷新后看板仍能看到导入的所有事项，状态和内容与导入后完全一致，数据未丢失

### 40. 回归测试（node test.js）
1. 打开终端，进入项目根目录
2. 运行命令：`node test.js`
3. ✅ **预期结果**：
   - 终端显示所有测试用例，包括新增的 6 个异常交接包测试
   - 所有测试用例均显示 ✅ 通过，包括：
     - ✅ 导出异常交接包 - 字段完整性
     - ✅ 校验异常交接包结构 - 字段缺失提示
     - ✅ 导入预览 - 新增/覆盖/冲突/跳过 四类场景
     - ✅ 权限校验 - 角色权限 + 候选人列表双重拦截
     - ✅ 执行导入 - 新增 + 覆盖 + 冲突跳过，写入恢复日志，刷新持久化
     - ✅ 冲突场景 - 同编号版本不同 / 本地有更新，导入被跳过
   - 最终显示「🎉 所有测试通过！」

### 41. 班前检查模板配置（班长权限）
1. 以张三（班长）登录，点击「⚙️ 配置」→「班前检查模板」
2. 点击「+ 新建模板」，输入模板名称（如"标准班前检查"）
3. 选择适用班次（不选则所有班次通用）
4. 添加检查项，勾选「必填」标记关键字段
5. 点击「保存模板配置」
6. ✅ **预期结果**：模板保存成功，刷新页面后仍能看到

### 42. 生成检查单（值班员执行）
1. 以李四（运维值班员）登录，点击「✅ 班前检查」
2. 点击「📋 生成检查单」，选择模板后确认
3. ✅ **预期结果**：生成成功，在「进行中的检查单」区域显示
4. 逐项勾选检查项，可填写备注
5. 不勾选必填项时点击「✅ 完成检查」
6. ✅ **预期结果**：弹出提示"还有 X 个必填项未完成"，列出未完成项名称

### 43. 权限拦截场景
1. 以赵六（观察员）登录，点击「✅ 班前检查」
2. ✅ **预期结果**：页面顶部显示"观察员无权限执行班前检查"，看不到「生成检查单」按钮
3. 尝试切换到配置页的「班前检查模板」
4. ✅ **预期结果**：看不到「新建模板」按钮，无法编辑模板内容

### 44. 重复生成当天检查单拦截
1. 以同一用户、同一模板、同一天再次点击「📋 生成检查单」
2. ✅ **预期结果**：弹出提示"当天已生成过该模板的检查单，不能重复生成"

### 45. 模板被删除后生成检查单
1. 在配置页删除一个模板
2. 返回班前检查页，尝试用已删除的模板生成检查单
3. ✅ **预期结果**：弹出提示"检查清单模板已被删除，无法生成检查单"

### 46. 完成检查单与操作日志
1. 勾选所有必填项后点击「✅ 完成检查」
2. ✅ **预期结果**：检查单移到「已完成的检查单」区域
3. 切换到「💾 备份恢复」→「恢复日志」
4. ✅ **预期结果**：能看到一条"完成班前检查"的日志记录，含操作人、模板名、总项数

### 47. 刷新页面后数据持久化
1. 刷新浏览器（F5）
2. ✅ **预期结果**：
   - 进行中和已完成的检查单仍然存在
   - 勾选状态和备注信息完整保留
   - 已完成的检查单不能再修改

### 48. 导出检查记录 JSON/CSV
1. 在「✅ 班前检查」页点击「📤 导出JSON」
2. ✅ **预期结果**：下载 JSON 文件，包含班次、执行人、完成时间、未通过项、备注等字段
3. 点击「📊 导出CSV」
4. ✅ **预期结果**：下载 CSV 文件，Excel 可直接打开，中文不乱码

### 49. 导出空记录提示
1. 清空所有检查记录后点击导出
2. ✅ **预期结果**：弹出提示"没有可导出的检查记录"，不生成文件

### 50. 回归测试覆盖班前检查清单
1. 运行 `node test.js`
2. ✅ **预期结果**：第 10 节"班前检查清单模块测试"共 16 项全部通过，包括：
   - ✅ 模板创建/查询/更新/删除
   - ✅ 生成检查单字段验证
   - ✅ 权限拦截（观察员）
   - ✅ 重复生成拦截
   - ✅ 模板删除后生成提示
   - ✅ 勾选/备注/非本人修改拦截
   - ✅ 必填项未完成拦截
   - ✅ 完成后写入恢复日志
   - ✅ 已完成不可再修改
   - ✅ 刷新后数据持久化
   - ✅ JSON/CSV 导出
   - ✅ 空记录导出提示

---

## 十一、临时协同处置室模块（第 51~75 步）

### 51. 入口验证：导航栏 Tab 和看板拉起按钮
1. 以李四（运维值班员）登录
2. 观察顶部导航栏
3. ✅ **预期结果**：顶部新增「🚨 协同处置室」Tab 按钮
4. 在看板中点击任一事项（如「光模块温度异常」）打开详情弹窗
5. 观察弹窗底部 footer 区域
6. ✅ **预期结果**：底部出现「🚨 拉起处置室」按钮

### 52. 从看板事项一键拉起处置室
1. 在事项详情弹窗点击「🚨 拉起处置室」
2. ✅ **预期结果**：弹出创建处置室弹窗
   - 顶部提示「从看板事项一键拉起，已自动关联事项标题」
   - 标题输入框已预填关联事项的标题
3. 填写表单：
   - 事件级别：选择「紧急」
   - 影响范围：输入「核心机房 A 区 3 号机柜，影响 2 条业务链路」
   - 处置目标：输入「2 小时内更换光模块，温度降至 50℃ 以下，链路稳定」
   - 截止时间：选择 2 小时后的时间
   - 参与人：勾选「张三（班长）」和「王五（值班员）」
4. 点击「🚨 创建并进入处置室」
5. ✅ **预期结果**：
   - 创建成功后自动跳转到「🚨 协同处置室」Tab
   - 自动打开该处置室的详情弹窗
   - 顶部弹出成功提示：「处置室已创建，自动进入详情」

### 53. 处置室列表页 - 筛选栏和卡片展示
1. 关闭详情弹窗，查看列表页
2. ✅ **预期结果**：列表页顶部显示筛选栏：
   - 状态下拉（全部/进行中/已关闭）
   - 级别下拉（全部/紧急/高/中/低）
   - 参与人下拉（全部/各个用户）
3. 查看处置室卡片
4. ✅ **预期结果**：
   - 卡片左侧显示彩色色条（紧急=红色，高=橙色，中=黄色，低=绿色）
   - 卡片顶部显示状态徽章（蓝色「进行中」或灰色「已关闭」）
   - 卡片显示级别徽章（对应颜色）
   - 卡片显示参与人 chip（带姓名首字）
   - 卡片显示截止时间、创建时间、创建人
   - 若有待确认问题，显示黄色「X 个待确认」计数

### 54. 按状态、级别、参与人筛选
1. 在筛选栏「状态」选择「进行中」
2. ✅ **预期结果**：只显示进行中的处置室
3. 在「级别」选择「紧急」
4. ✅ **预期结果**：只显示级别为紧急的处置室
5. 在「参与人」选择「张三（班长）」
6. ✅ **预期结果**：只显示参与者包含张三的处置室
7. 依次切换回「全部」
8. ✅ **预期结果**：筛选条件实时生效，卡片数相应变化

### 55. 普通值班员补充进展（含文字附件）
1. 点击处置室卡片，打开详情弹窗
2. 确认当前登录用户是李四（值班员），且在参与人列表中
3. 在底部消息输入区，填写进展内容：「已联系机房现场，准备备件光模块」
4. 点击「📎 上传文字附件」按钮
5. 填写附件弹窗：
   - 附件名称：现场检查记录
   - 附件内容：机柜温度 32℃，光模块 Tx 功率异常偏低，已确认型号兼容
6. 点击「附加到消息」，再点击「✅ 提交进展」
7. ✅ **预期结果**：
   - 消息流中出现一条绿色头像的进展消息
   - 消息包含提交时间、操作人、内容
   - 附件以可展开/收起的卡片形式展示在消息下方
   - 右侧操作日志新增一条「补充进展」记录

### 56. 普通值班员提出待确认问题
1. 在消息输入区切换到「❓ 提出待确认问题」Tab
2. 输入问题内容：「是否需要同时申请备用链路临时调度？」
3. 点击「✅ 提交问题」
4. ✅ **预期结果**：
   - 消息流中出现一条黄色头像的问题消息
   - 右侧「待确认问题」区域新增一条卡片，显示状态「待答复」
   - 操作日志新增一条「提出待确认问题」记录

### 57. 班长答复待确认问题
1. 顶部切换当前用户为「张三（班长）」
2. 打开同一处置室详情
3. 在右侧「待确认问题」区域点击「✍️ 答复」按钮
4. 输入答复内容：「同意，立即申请备用链路调度，通知运营商配合」
5. 点击确认
6. ✅ **预期结果**：
   - 待确认问题卡片状态变为「已答复」，背景色由黄变灰
   - 问题下方展示答复内容和答复人
   - 消息流中出现青色头像的答复消息，关联引用原问题
   - 操作日志新增一条「答复待确认问题」记录

### 58. 班长调整参与人
1. 仍以张三（班长）登录，在处置室详情页右上角点击「⚙️ 管理：调整参与人」
2. 勾选面板中新增「赵六（观察员）」，取消勾选「王五（值班员）」
3. 点击「✅ 确认调整」
4. ✅ **预期结果**：
   - 参与人列表实时更新：保留张三、李四，新增赵六，移除王五
   - 右侧操作日志新增两条：「添加成员：赵六」和「移除成员：王五」
   - 参与人 chip 同步更新

### 59. 权限拦截 - 普通值班员尝试调整成员
1. 切换回李四（值班员）登录
2. ✅ **预期结果**：处置室详情页右上角**看不到**「⚙️ 管理：调整参与人」按钮

### 60. 权限拦截 - 非参与者尝试提交消息
1. 切换到王五（值班员）登录（已在步骤 58 中被移除参与人）
2. 进入该处置室详情
3. ✅ **预期结果**：
   - 页面顶部显示红色权限提示：「您不是该处置室参与人，仅可查看信息」
   - 底部消息输入区被隐藏（灰色提示），无法提交进展、问题、附件

### 61. 班长编辑基本信息
1. 切换到张三（班长）登录
2. 点击「⚙️ 管理：编辑基本信息」按钮
3. 修改标题为「【已定位根因】光模块温度异常」，修改事件级别为「中」
4. 点击「✅ 保存修改」
5. ✅ **预期结果**：
   - 标题、级别实时更新
   - 卡片左侧色条由红色变为黄色（对应「中」级别）
   - 操作日志新增「修改基本信息」记录，列出变更字段

### 62. 班长关闭处置室
1. 仍以张三（班长）登录，点击「📌 关闭处置室」按钮
2. 填写关闭原因：「已更换光模块，温度降至 42℃，端口连续 30 分钟稳定，业务恢复正常」
3. 点击「✅ 确认关闭」
4. ✅ **预期结果**：
   - 状态徽章立即变为灰色「已关闭」
   - 页面顶部显示蓝色提示条：该处置室已关闭，可由班长重新开启
   - 底部消息输入区显示「处置室已关闭，无法新增消息」
   - 操作日志新增「关闭处置室」记录，包含关闭原因

### 63. 已关闭处置室 - 拦截继续编辑
1. 仍在已关闭的处置室详情页
2. ✅ **预期结果**：
   - 底部输入区灰色禁用，任何提交按钮不可点击
   - 「⚙️ 管理」按钮全部隐藏
   - 仅「📌 重新开启处置室」按钮可见（仅班长可见）

### 64. 冲突 1：同一事项重复拉起处置室
1. 回到看板，点击刚才关联的**同一个事项**打开详情
2. 点击底部「🚨 拉起处置室」按钮
3. ✅ **预期结果**：弹出红色冲突提示：「该事项已有关联的进行中处置室，请勿重复拉起，点击查看已有处置室」
4. 点击提示中的查看链接
5. ✅ **预期结果**：自动跳转到处置室详情

### 65. 冲突 2：已关闭后允许重新拉起同一事项（不冲突）
1. 先确保该事项的处置室处于「已关闭」状态（如步骤 62）
2. 再次回到看板，点击同一个事项详情，点击「🚨 拉起处置室」
3. ✅ **预期结果**：不拦截，正常弹出创建弹窗（因为已关闭的不视为重复）

### 66. 班长重新开启已关闭的处置室
1. 以张三（班长）打开已关闭的处置室详情
2. 点击「📌 重新开启处置室」按钮
3. ✅ **预期结果**：
   - 状态立即恢复为「进行中」
   - 顶部蓝色提示条消失
   - 消息输入区恢复可用
   - 操作日志新增「重新开启处置室」记录
4. 提交一条新进展
5. ✅ **预期结果**：可正常提交消息，状态和版本号正常更新

### 67. 冲突 3：乐观锁版本冲突（模拟双页面）
1. 同时打开两个浏览器窗口/tab，都进入同一个处置室详情页
2. 在窗口 A 中提交一条进展消息（版本号提升）
3. 回到窗口 B（此时仍停留在旧版本），尝试提交另一条消息或修改基本信息
4. ✅ **预期结果**：弹出红色冲突提示：「版本冲突，已有他人更新，请刷新后重试」
5. 刷新窗口 B
6. ✅ **预期结果**：窗口 B 能看到窗口 A 提交的所有消息，再次提交正常

### 68. 导出单个处置室为 JSON 摘要
1. 打开任一处置室详情页
2. 点击顶部「📤 导出 JSON」按钮
3. ✅ **预期结果**：下载 JSON 文件，文件名包含处置室 ID 和日期，包含：
   - 基本信息（id/标题/级别/状态/影响范围/目标/截止时间）
   - 参与人列表
   - 消息流水（messages 数组，区分进展/问题/答复）
   - 待确认问题清单
   - 统计信息（totalMessages/totalQuestions 等）
   - 操作日志（operationLogs 数组）

### 69. 导出单个处置室为 CSV 摘要
1. 点击「📊 导出 CSV」按钮
2. ✅ **预期结果**：下载 CSV 文件，带 UTF-8 BOM，Excel 可直接打开且中文不乱码
3. 用 Excel 打开查看
4. ✅ **预期结果**：CSV 分 4 个区：
   - **基本信息区**：处置室 ID、标题、级别、状态、创建人、创建时间、关闭时间、参与人等
   - **消息流水区**：每条消息一行，包含时间、类型、作者、内容、附件
   - **问题清单区**：每个问题一行，包含提问人、内容、答复人、答复内容、状态
   - **操作日志区**：每条日志一行，包含时间、动作、详情、操作人

### 70. localStorage 持久化（刷新/重新打开浏览器）
1. 在处置室模块中完成若干操作：创建、提交进展、提问题、答问题、关闭等
2. 记录当前处置室的状态、消息数量、版本号等关键信息
3. 按 F5 刷新浏览器页面
4. ✅ **预期结果**：页面重新加载后：
   - 顶部导航仍停留在「🚨 协同处置室」Tab
   - 列表页所有卡片完整，状态/级别/参与人与刷新前一致
   - 打开详情后，消息流、问题、日志、附件全部保留，无丢失
   - 版本号与刷新前一致
5. （可选）关闭整个浏览器，重新打开页面
6. ✅ **预期结果**：所有数据仍然完整存在，不会因关闭浏览器丢失

### 71. 完整导出/备份中包含处置室数据
1. 以张三（班长）登录，点击「💾 备份恢复」→「📤 导出完整交接包」
2. 用文本编辑器打开导出的 JSON 文件
3. ✅ **预期结果**：文件顶层包含 `collabRooms` 数组字段，数组元素数量与当前处置室数量一致

### 72. 完整恢复/导入包含处置室数据
1. 清空当前处置室列表（或用新浏览器 profile）
2. 在「💾 备份恢复」页导入步骤 71 中导出的完整交接包
3. 预览窗口中查看 summary 区域
4. ✅ **预期结果**：summary 中显示「处置室 X 条」数量正确
5. 确认导入后切换到「🚨 协同处置室」Tab
6. ✅ **预期结果**：所有处置室完整恢复，详情打开后消息、问题、附件、日志均存在

### 73. 恢复日志中显示处置室对比
1. 导入完成后，到「💾 备份恢复」→「恢复日志」区域
2. 找到刚执行的导入记录，展开查看
3. ✅ **预期结果**：对比网格中包含「处置室」行，显示导入前后的数量对比

### 74. 四类冲突提示清晰明确
对以下 4 类操作，用户都能看到**明确、易懂、带具体指引**的提示：
- **duplicate_room**（重复拉起）：提示中含「已有进行中处置室」字样和一键查看链接
- **permission**（无权限）：提示中说明具体原因（观察员无创建权/非参与者/非班长）
- **closed**（已关闭）：提示中说明「处置室已关闭，无法继续编辑」
- **version**（版本冲突）：提示中建议「刷新页面后重试」，并返回最新数据

### 75. 回归测试覆盖处置室（test.js）
1. 打开终端，进入项目根目录
2. 运行命令：`node test.js`
3. ✅ **预期结果**：
   - 第 11 节「临时协同处置室测试」共 26 项全部通过
   - 涵盖：数据模型、创建、重复拉起、权限、查询、权限方法、
     添加进展带附件、非参与者拦截、问题/答复、成员调整（含权限）、
     乐观锁（拦截+正常）、关闭、已关闭拦截、关闭后重复拉起允许、
     重新开启、重新开启后提交消息、JSON 导出、CSV 导出、
     localStorage 持久化、完整导出含 collabRooms、备份恢复完整流程、
     四种冲突类型全流程验证
   - 最终显示「🎉 所有测试通过！」，总计通过数应 ≥ 134

## 核心技术实现

### 数据持久化
使用浏览器 `localStorage` 进行数据存储，所有数据以 JSON 格式序列化存储。

### 版本冲突检测
采用乐观锁机制，每次修改时携带基础版本号，如果与当前版本不符则拒绝修改并返回冲突信息。

### 权限控制
基于角色的访问控制（RBAC），每个角色有独立的权限标志，操作前校验当前用户角色是否具备相应权限。

### 状态流转
事项状态流转图：
```
新建 → 处理中 → 待接班确认 → 已接收
 ↓        ↓          ↓          ↓
  --------- 可随时关闭 --------→ 已关闭
```

## 常见问题

**Q: 数据存储在哪里？会丢失吗？**
A: 数据存储在浏览器的 localStorage 中。只要不清除浏览器数据，数据就会一直保留。

**Q: 可以在多台设备间同步数据吗？**
A: 当前版本仅支持本地存储，不支持多设备同步。如需同步功能，需要后端服务支持。

**Q: 如何备份数据？**
A: 点击顶部导航栏的「💾 备份恢复」按钮进入备份与恢复中心，点击「📥 导出完整数据包」按钮即可下载完整的 JSON 格式备份文件。恢复时选择本地备份文件，经过校验和预览后即可恢复数据。只有具备配置管理权限的用户（如班长）才能执行恢复操作。

**Q: 支持多用户同时在线编辑吗？**
A: 不支持，本应用是单机版应用。但内置了版本冲突检测机制，防止同一事项被覆盖修改。
